// Create a function args_count, that returns the count of passed arguments
function args_count (a,b,c) {
   // tengo un string
    cuento quantos objetos hay en el string
    doy el numero de objetos del string
}

console.assert(args_count(1, 2, 3) === 3, "args_count should return 3 when given (1, 2, 3)");
console.assert(args_count(1, 2, 3, 10) === 4, "args_count should return 4 when given (1, 2, 3, 10)");