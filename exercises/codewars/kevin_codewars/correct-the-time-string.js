module.exports = timeCorrect;

function timeCorrect(time) {
time.split(":")

    let hours = time[0];
    let minutes = time[3,4];
    let seconds = time[6][7];
    // if (hours < 25 && minutes < 61 && seconds < 61) {
        return hours ;
 }
